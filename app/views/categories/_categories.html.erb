<div class="col-md-3">
    <p class="lead"><%= t('categories') %></p>
    <div class="list-group">
    <% @categories.each do |category| %>
      <%= link_to category.title, category, class: "list-group-item" %></a>
    <% end %>
    </div>
</div>